{
    "name": "lights-out",
    "version": "1.1.5",
    "type": "module",
    "scripts": {
        "clean": "rimraf --glob public_html/scripts/*",
        "pack": "npx webpack --mode production",
        "zip": "node scripts/zip.mjs",
        "build": "npm run clean && npm run pack && npm run zip"
    },
    "devDependencies": {
        "rimraf": "^6.1.3",
        "ts-loader": "^9.6.2",
        "webpack": "^5.108.4",
        "webpack-cli": "^7.2.1"
    },
    "dependencies": {
        "jszip": "^3.10.1"
    }
}
